@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# arcgen depends on CGAL and Boost headers; make consumers find them too
find_dependency(CGAL REQUIRED)
find_dependency(Boost REQUIRED)

# Import our exported targets (from install tree)
include("${CMAKE_CURRENT_LIST_DIR}/arcgenTargets.cmake")
